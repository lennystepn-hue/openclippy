import { ClippyWidget } from './clippy'

const container = document.getElementById('clippy-container')
if (container) {
  const clippy = new ClippyWidget(container)
  // Will be expanded with chat integration later
}
